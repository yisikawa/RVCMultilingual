# 多言語RVCアプリの低コスト・高品質構成案

Gemini APIによる高度な翻訳・台本生成を核とし、Google Cloud TTSとRVCを組み合わせることで、実用的なコストで最高品質の多言語ボイス出力を実現するプランです。

## 概要

Google Cloud TTSの高品質な音声（Neural2 / Studio等）をベース音声として活用し、特定のキャラクターの声へとRVCで変換します。

## 構成案とコスト比較

| コンポーネント | 推奨（低コスト・高品質） | 費用目安 | 代替案（無料） |
| :--- | :--- | :--- | :--- |
| **言語処理** | **Gemini 2.0 Flash** | 無料枠内 | (同左) |
| **音声合成 (TTS)** | **Google Cloud TTS** | 無料枠あり（従量課金） | `edge-tts` (無料) |
| **声質変換 (RVC)** | **RVC v2** (Local / Cloud) | $0 / $0.4/h | (同左) |
| **API管理** | **Firebase / Vercel** | 無料枠内 | (同左) |

## 各コンポーネントの選定理由

1.  **Google Cloud TTS**:
    *   **Neural2 / Studio 音声**: 非常に自然で、多言語対応も非常に強力です。
    *   **コスト**: 月間一定量まで無料で利用できる枠があり、OpenAI TTSと比較しても非常に経済的です。RVCのインプットとして理想的です。
2.  **Gemini 1.5 Flash (翻訳・リライト)**:
    *   「Google Cloud TTSで読み上げるための、フランス語の自然な台本」を生成。
3.  **RVC v2 (声質変換)**:
    *   Google TTSの高品質な「滑舌とイントネーション」を維持したまま、ターゲットボイスに変換。

## GUIアプリ（Streamlit）による統合インターフェース

ユーザーが直感的に操作できるよう、**Streamlit** を使用して1画面で全てのプロセスを完結させます。

### 画面構成イメージ
1.  **モデル・アプリ設定エリア**: RVCモデルパス、APIキー等の指定。**設定をJSON保存する機能**を追加。
2.  **入力エリア**: 日本語セリフの入力、キャラクター設定。
3.  **言語・音声選択**: 翻訳先言語とGoogle TTS音声の選択。
4.  **TTSプレビュー・実行**: 翻訳結果とベース音声の生成・再生。
5.  **RVC変換ボイスエリア**: 変換された最終音声の再生および保存（ダウンロード）。

### 技術スタック（UI）
*   **Framework**: Streamlit
*   **Persistence**: `json` モジュールによる設定ファイルの書き出し・読み込み

## 実装のステップ
1.  **設定保存ロジック**: `config.json` に設定を保存し、次回起動時に自動読み込みする機能を実装。
2.  **RVC出力ファイルの検知・再生**: 指定された出力パスにある音声をUI上で再生・保存できるように `app.py` を拡張。
3.  **UXの向上**: 1画面で「翻訳 -> TTS作成 -> (外部変換後) -> 最終確認」のフローをスムーズにする。

## メリット
*   **圧倒的なコスパ**: Google Cloudの無料枠を活用でき、かつ品質は世界最高峰。
*   **Googleエコシステムの親和性**: GeminiとのAPI管理の統合が容易です。
